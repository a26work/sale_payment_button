<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Record Rule for Sales Users: Own Documents Only -->
        <record id="payment_sale_order_own_rule" model="ir.rule">
            <field name="name">Payments from Own Sales Orders</field>
            <field name="model_id" ref="account.model_account_payment"/>
            <field name="domain_force">[
                '|',
                ('partner_id.user_id', '=', user.id),
                ('reconciled_invoice_ids.invoice_line_ids.sale_line_ids.order_id.user_id', '=', user.id)
            ]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>
    </data>
</odoo>